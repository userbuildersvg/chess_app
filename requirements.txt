fastapi==0.128.8
uvicorn[standard]==0.39.0
pydantic==2.13.5
chess==1.10.0
httpx==0.28.1
requests==2.32.5
# Postgres (Neon) - replaces the two SQLite files that lived on Render's
# ephemeral disk. [binary] avoids needing libpq and a compiler in the image;
# [pool] is the connection pool db.py opens.
psycopg[binary,pool]==3.2.13
